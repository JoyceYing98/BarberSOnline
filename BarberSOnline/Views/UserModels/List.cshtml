@model IEnumerable<BarberSOnline.Models.UserModel>

@{
    ViewData["Title"] = "List";
    Layout = "~/Views/Shared/_LayoutUser.cshtml";
}

<h1>Report</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Username)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Temperature)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Visit)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>

        <tr>
            <td>
             @ViewBag.UserId
            </td>
            <td>
              @ViewBag.Temperature
            </td>
            <td>
                @ViewBag.Status
            </td>
            <td>
                @ViewBag.Visit
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@ViewBag.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@ViewBag.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@ViewBag.ID">Delete</a>
            </td>
        </tr>

    </tbody>
</table>
